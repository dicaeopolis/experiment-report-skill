---
name: experiment-report-skill
description: 从已完成的实验材料创建课程风格实验报告 LaTeX 项目，遵循严格的来源限界和面向读者的约束。用于将完成的实验资产转换为 Overleaf 就绪的报告包，包含 main.tex 及所需的图表和参考文献文件。
compatibility: opencode
---

# 实验报告技能

## 概述

从已完成的实验材料创建独立的 LaTeX 报告项目。最终成品应符合课程实验报告风格，使得未接触项目中间过程的教师可直接阅读理解，并在存在 XeLaTeX 环境的条件下生成可交付的 PDF 格式报告，或者在 Overleaf 上通过 `main.tex` 主入口文件直接编译。

## 核心原则

课程实验报告是叙述"在实验过程中，我们做了什么"，不是"已知什么"的目录，也不是行动清单或者其他类似物。教师阅读它是为了跟随实验的决策链——每一步为什么采取、出现了什么障碍、如何解决、结果意味着什么。如果一份报告可以由读过教科书但没有做过实验的人写出来，那它就失败了。

也就是说，总体的写法应当是具有学术专业性的叙述文体。负例：一份关于标准教科书主题的报告描述的是与教科书相同的算法；正例：从实验者的视角出发："我们选择这个数据结构是因为……"而非"某算法由……组成"。最简单的检验：问自己每一句话是从做出设计决策、收集了测量数据的人的角度写的，还是从解释某个概念或者解释某种操作的人的角度写的。对实验者的动作用主动的、决策导向的动词（选择、测量、决定、实现），对算法本身保留定义性动词。当项目特有的陈述更准确时，避免读起来像教科书定义的语言。这并非杜绝教科书语言。当阐述通用的实验原理、公式等内容时，由于不涉及实验过程，使用正确的教科书语气是应当的。

## 工作流

### 0. 起草前检查教师下发的实验讲义

- 起草前，查找包内是否存在教师下发的实验讲义、实验手册、检查清单、评分标准、任务描述或类似的要求文件。
- 如果存在这样的文件，先阅读它，将其转化为一个具体的完成清单，列出所需的实验阶段、产出物、对比、分析和提交物。
- 将该清单与实际实验材料、代码、输出、日志、图表、导出文件和保存记录进行比对。
- 如果材料未显示要求的实验已完全完成，停止并告知用户实验不完整。列出缺失或未验证的项目，而不是生成一份将实验呈现为已完成的报告。
- 仅当手册驱动的实验要求已满足时，或用户明确要求一份明确标记为部分完成的草稿时，才继续起草。
- 如果没有教师下发的讲义或检查清单，按以下默认工作流继续。

### 1. 起草前恢复原始材料

- 在选择结构或用词之前，先阅读实验材料。
- 确认笔记、PDF、截图、导出图形和约束条件均可读。
- 如果本地文本或图像提取出现乱码、结构性损坏或不完整，请考虑切换读取方式，若仍不成功，请停止并告知用户。
- 如果本地恢复工具被沙箱阻止，直接请求升级。
- 经验证的本地命令模板见 [references/source-recovery.md](references/source-recovery.md)。

### 2. 挖掘实验证据背后的内容

- 将材料分组为任务背景、方法、实现、实验设置、结果、分析和结论。
- 将每一项重要声称绑定到一个具体的来源条目，如结果表、图形、运行日志、配置文件或用户提供的说明。当实验涉及编写或修改代码时，主要证据是项目自身的源文件、提交和配置，而非算法的外部教科书描述。
- 也就是说，实验报告应当是“照着材料讲”。如果我要说明算法流程，就最好在前面贴出来算法框架和核心代码，然后照着这些代码讲；如果我要比较两个模型，就最好在前面把模型跑出来的指标表格，图线趋势等贴出来，再照着它们的信息讲。这样读者对行文会更明晰，不至于反复翻查引用。这一节和后文第八节要求类似。
- 如果材料不支持某项声称，写出关于已知内容的有限描述，而非超出证据的推断，如果能够基于现有实验材料分析推断，则请做相关的验证。
- 注意不能写成证据罗列。材料证据和数据从来不能占大头，不然，实验报告就变成日志汇总了，这很荒谬。重点在于思考背后的内容，说明什么，反映什么，对后面的操作有何影响，等等。
- 更严格的来源边界规则见 [references/evidence-grounding.md](references/evidence-grounding.md)。

### 3. 为读者理解而写

- 为没有见过用户中间工作的教师而写，教师应能直接跟随报告。
- 保留面向读者的写作中有用的部分：定义新术语，局部解释符号，使每张图和表在无需隐藏上下文的情况下可理解。
- 从报告正文中删除演示者视角的语言、计划语言、阅读指导和流程笔记。
- 不要使用幻灯片式过渡或口头填充词，如"下面我们来看"或"这一部分主要说明"。
- 无需写入项目的排错与修复过程，除非明确要求。
- 也就是说，总体行文风格应当是“专业而详尽的流畅行文”。下面来解释一下：
  - “专业”：意思是整体风格符合实验报告的规范，不使用口语化、文学化或者修辞化的句子。正例：“本算法在该场景下取得xx%的提升，主要得益于某架构带来的硬件加速”；负例：“有了某架构的硬件加速，本算法就吃到了xx%的提速，这很香”。
  - “详尽”：意思是使用完善详尽的语言，让陈述自由伸展，不滥用电报式语言或者无明确意义的缩写。正例：“C 语言端的测试使用 FIPS 197 Appendix C 和 NIST SP800-38A 规定的的标准测试向量进行自测。同时也在 Python 端利用用 pycryptodome 的 AES 标准实现库，生成 200 组随机密钥-明文对，分别核对加密输出、解密输出和加密后解密的 roundtrip 结果，实现交叉核验。”；负例：“C 端用FIPS 197 Appendix C 和 NIST SP800-38A 测试向量测。Python pycryptodome 跑 200 组随机密明对，核加解密输出、roundtrip。”
  - “流畅”：意思是语言富有内在逻辑，读起来自然有承接，而不是生硬罗列、术语堆积或者逻辑跳脱。
- 完整的审阅标准见 [references/final-review.md](references/final-review.md)。

### 4. 从材料构建报告，而非从记忆

- 将提供的实验材料作为主要内容边界。
- 外部引用仅用于标准术语、引用元数据，或所供材料已依赖的背景事实。
- 不要编造材料中不存在的结果、实现细节、对比或结论。
- 使每个章节在无法访问用户私密工作上下文或先前聊天记录的情况下仍可理解。

### 5. 默认使用课程报告结构

- 优先使用标准结构，如：实验目的或任务背景、方法与实现、实验设置、实验结果、结果分析、结论，以及需要时的参考文献或附录。
- 根据材料调整章节深度。不要仅为模仿模板而创建空的形式化章节。
- 默认结构模式见 [references/report-structure.md](references/report-structure.md)。

以下强制性条目无论用户是否提供教师下发的检查清单或评分标准均适用。

- 如果用户提供了教师下发的报告检查清单或评分标准，按该结构而非通用默认结构执行。在可行的前提下使章节名称和顺序与教师的措辞一致。
- 省略仅面向教师的部分，如评分意见或分数，除非用户明确要求保留占位符。
- 将文档标题视为实验名称，当这满足教师对简明实验名称的要求时。
- 除非用户明确提供不同的身份元数据，默认报告作者行为 `<单位> <姓名> <学号>`。如果所选模板将姓名、单位和学号分开到不同字段，将此默认作者身份自然映射到这些字段中。
- 建议包含一张同时涵盖软件和硬件环境详情的表格。如果本地系统查询被阻止，直接请求升级而非猜测。
- 如果教师仅要求程序和设计文件与报告一起提交，使用简短的面向教师的说明如 `编写的完整内容请参见随附代码`，而非列出内部资产名称。
- 当教师只需要流程、步骤和方法时，保持实验方案设计简明。不要增加流程图，除非教师明确要求。

### 6. 仅在明确要求时处理代码补全与运行类实验

- 若通过本节条件判断，则覆盖 report-structure.md 的默认结构，而是采用下面的结构：
- 仅当用户明确说实验依赖于填写代码、运行代码、观察输出、然后撰写结论时才使用此路径。不要仅因为材料包含代码就为普通报告推断此路径。如果你不确定，请向用户提问。
- 对此路径，当与作业更匹配时，优先使用 `实验内容` 节而非纯总结式的 `实验结果` 节。按任务或子任务组织，对每个任务说明完成了什么代码、运行了什么、产生了什么结果、得出了什么结论。
- 将其写为自然的任务叙述，例如 `任务 1.2 中，程序实现……。运行后得到……，说明……。` 不要使用可见的元标签，如 `代码补全内容`、`运行输出`、`结论填写依据` 或类似的面向写作者的小标题。
- 当展示手动完成的代码时，包含实际填写的函数或实际程序块以支撑报告。不要将简化片段当作提交的代码呈现。
- 避免暗示代码被隐藏或缩写的措辞，如 `核心实现`、`核心计算`、`核心更新`、`主要代码如下` 或 `代码片段如下`。优先使用直接措辞如 `函数实际填写为`、`代码填写为` 或 `循环填写为`。
- 不要机械地禁用"核心"一词。当语义上正确时，保留正常任务术语如 `关键核心指标` 或硬件描述如 `CPU 逻辑核心数`。

### 7. 明确处理术语、公式、图形和表格

- 对有稳定中文翻译的术语使用中文名称；对无稳定中文翻译的领域标准术语保持其标准形式。
- 在首次提及时解释技术术语。
- 在首次出现时或紧邻处定义每个新的数学符号。
- 当需要公式时，保留核心公式和教师侧理解所需的最短推导链。
- 每张图和表应有明确的作用、局部解释以及不过度陈述其内容的文本结论。

### 8. 本地编译，验证图形完整性，然后生成 Overleaf 就绪项目

- **先本地编译**：运行 `xelatex -interaction=nonstopmode main.tex` 两次（用于交叉引用），如果存在 `.bib` 文件则运行 `bibtex main && xelatex main.tex && xelatex main.tex`。在继续之前修复所有编译错误。
- 编译通过后**验证无死资产**。扫描报告项目目录中所有非 TeX、非编译产物的文件（图像、书目、数据文件等）。每个此类文件必须通过文档源码中的某个引用可达——无论是 `\includegraphics`、`\input`、`\bibliography` 或任何其他引入命令。放置在报告目录中但源码无任何行引用的文件是死资产。检查针对源码，而非目录：一个装满复制文件但零对应引用的目录是失败，无论资源组织得多么整洁。检测到死资产后，不应删除资产，而是在报告和合适位置添加对资产的引用和说明，借以丰富报告的完整性。
- 当本地编译干净（0 错误，仅允许字体/断词警告）且图形引用完整性通过后，可以交付。
- Overleaf 打包仅限于本地缺少 XeLaTeX 环境时触发。
- 干净的本地 PDF 证明包将在 Overleaf 上编译而不触及编译限制。
- 输出一个可直接上传到 Overleaf 的报告包。
- 默认主入口文件名为 `main.tex`，除非用户指定其他文件名或模板。
- 对图形和其他资产使用相对路径。

### 9. 交付前的独立合规审计

- 本地编译和死资产检查通过后，按顺序执行两轮审计。

**Pass A — 全局审计（轻量 agent）**。生成一个廉价、快速的 agent （例如fast/explore或者mini等模型/子代理）检查两个全局属性。从假设报告两项均未通过开始。你的任务是找到它通过的具体证据——不是给它 benefit of the doubt。对每一项检查施加侵略性判断：如果一个段落本应该承担叙述实验过程的任务，却错误写成了内容、技术或者结果的罗列，或者单纯平淡缺乏深度，就标记它。详细要求如下：

第一，叙述声音。报告必须以实验者的视角书写，而非教科书作者的视角。应用以下测试：阅读该段落时，是否真正在展示作者需要做什么，在做什么，为什么做这个，效果如何，和前后衔接如何（这一系列问题不是必然要求项，不必让每个段落都承担所有的这些任务，这些主要是列出审计方向，如果一个段落很少完成这些任务，才值得怀疑。特别注意：“实验原理”部分，不受到这个的约束，实验原理反而应该以教科书语气叙述），是否做到了“专业而详尽的流畅行文”。再次解释该风格：
  - “专业”：意思是整体风格符合实验报告的规范，不使用口语化、文学化或者修辞化的句子。正例：“本算法在该场景下取得xx%的提升，主要得益于某架构带来的硬件加速”；负例：“有了某架构的硬件加速，本算法就吃到了xx%的提速，这很香”。
  - “详尽”：意思是使用完善详尽的语言，让陈述自由伸展，不滥用电报式语言或者无明确意义的缩写。正例：“C 语言端的测试使用 FIPS 197 Appendix C 和 NIST SP800-38A 规定的的标准测试向量进行自测。同时也在 Python 端利用用 pycryptodome 的 AES 标准实现库，生成 200 组随机密钥-明文对，分别核对加密输出、解密输出和加密后解密的 roundtrip 结果，实现交叉核验。”；负例：“C 端用FIPS 197 Appendix C 和 NIST SP800-38A 测试向量测。Python pycryptodome 跑 200 组随机密明对，核加解密输出、roundtrip。”
  - “流畅”：意思是语言富有内在逻辑，读起来自然有承接，而不是生硬罗列、术语堆积或者逻辑跳脱。

第二，分析深度。分析是否超越了读者仅从数据表就能推断的内容？标记任何结论仅改写表格而未添加解读的段落，任何数据模式或异常被观察到但未被解释的段落，或推理链被截断的段落。

在继续 Pass B 之前修复 Pass A 发现的所有问题，不要并行做两个审计。

**Pass B — 全面合规审计**。生成一个只读的 Oracle agent（`subagent_type="oracle"`）（或者xhigh等更重的子代理）对照本技能及其三个参考文件中的每一条约束审阅完整报告。此任务不要使用轻量 agent（explore、quick 等）——合规审计需要判断力，不是模式匹配。

从假设每一条约束均已被违反开始。对每条规则，报告必须证明合规——默认判定是违规，除非文本明确呈现了相反证据。不要给 benefit of the doubt。
- 审计 agent 必须从磁盘读取当前 `main.tex` 和完整的技能文件——而非依赖上下文记忆——并报告每条违规，附上具体的行号或节号以及它违反的确切规则。
- 写作 agent 必须在交付前解决每条报告的违规。解决不是自动接受：写作 agent 在回应每条审计发现时必须运用判断力。
- **审计发现分类**：当审计将一段文字标记为推测性或超越证据时，区分两种情况而非将所有"可能"或阐释性语言视为违规。
  1. **幻觉或不支持的主张**：文本断言了一个实验材料中不存在的事实、机制或结果。通过移除该主张或用具体证据支撑（日志数字、图形、配置设置）来修复。
  2. **有推理的分析或洞见**：文本提供了对观察结果的解释、一个合理的因果机制或一个以实验数据为基础的假设。这正是报告存在的目的——要传递的分析。不要将其压缩为一个更弱的陈述。验证推理链是否可见：观察应在解释之前陈述，推断步骤应是显式的而非折叠为一个扁平的模糊词如"可能因为X"。一个有效的洞见如果被写成单字模糊词仍然是写作失败——扩展它直到读者能够跟随从观察到推断的路径。优先扩展不清晰的推理而非删除它。
- **审计是确认门，不是修复循环**：本文件及三个参考文件中的所有技能约束都是起草时应内化的规则。写作 agent 应当产生一份已经遵守了每条约束的报告；审计捕捉残留遗漏。一份仅在大量来回修补后才通过审计的报告表明约束未被内化进起草过程。
- 请先确认“要向教师交付哪些资产（如代码、日志等）文件”，再划白名单区分交付资产和内部资产。不能一棍子把外部引用打死。
- 仅在审计干净通过后才继续打包。
- 此步骤在最终交付时运行一次。在写作阶段的局部编辑、增量添加或用户要求的微调后不要重新审计——仅编译检查和图形完整性复查即可。

## 面向教师的写作约束

- 永远不要假设教师能看到笔记本、日志、JSONL 文件、私密笔记、先前的聊天记录或内部工作流产物。
- 如果教师下发的实验讲义定义了强制实验阶段，除非材料确实显示这些阶段已完成，否则不要将报告写为这些阶段已完成。
- 不要写依赖隐藏上下文的短语，如 `实验记录显示`、`实验流程笔记显示`、`日志显示` 或类似的依赖于教师无法访问的后台材料的措辞。
- 不要暴露内部资产文件名、笔记本版本名称或仅用于流水线的文件名，除非用户明确要求在报告中列出。
- 在如 `实验目的`、`实验内容` 或 `任务背景` 等章节中，直接陈述实验任务和学习目标。不要将这些章节变为关于报告结构、文档对齐、包完整性或写作策略的评论。
- 从可见的报告文本中禁用后台标签，包括 `当前实验包中的`、`对齐版`、`新版 notebook`、导出目录名、包文件夹名及类似的内部标识符。
- 使用教师讲义决定完整度、结构和覆盖范围，但不要向报告正文写入可见的元短语，如 `根据手册要求`、`按文档要求` 或类似的面向教师的提醒，除非用户明确希望该措辞。
- 删除模糊的元组织短语，当它们不描述实验本身时，如 `面向课程文档的实验组织方式` 或类似措辞。
- 保留标准的课程原生术语，如 `dropout`、`embedding` 和 `batch size`，当它们是最自然的措辞时。避免生硬的英文携带词，如当有更直接的中文短语时避免 `sweep` 等表述。
- 报告正文禁止使用项目符号列表。将每条声称、观察和结论写为带逻辑链的连接性散文。唯一例外是：(a) 教师模板或用户明确要求列表，或 (b) 内容是对无逻辑排序或因果关系的独立条目的真正枚举（如物料清单）。存疑时使用散文。
- 删除面向写作者或评分者的元评论，如 `对于课程实验而言，这一点非常重要` 或类似的评论写作本身而非实验的表述。
- 当证据直接时优先使用直接的事实措辞。例如，将本地环境事实写为直接陈述而非模糊短语如 `结合本地运行环境信息，可以确认`。
- 保留解释实际实验差异的有意义的对比。仅删除低信息的修辞对比或演示者风格的转向短语。
- 避免重复的修辞对比模式，如 `不……而……`、`不代表……` 或类似的框架，当直接陈述能更干净地传达结果时。
- 详实不敷衍。报告中的分析应当推进到实验材料能够支撑的最大深度，不以"有待进一步分析"或类似表述作为分析的自然终点。如果对比的数据差异可以解释，就给出解释；如果反常结果支持一个假设，就把假设说清楚。一个简单的自检：如果读者仅看数据表格就能得出同样的结论，那这段分析没有增加任何信息。能力的边界是材料——材料能撑到什么深度，分析就写到什么深度，不以篇幅为停止条件。
- 当用户的目标是课程报告而非研究论文时，保持声称以证据为界，但避免不必要的论文式自我批评或过度强调局限性，除非教师明确要求。
- 当从实验数据中做出推断时，不要将推理折叠为一个扁平的模糊词。像"可能因为X"这样的短语表达了不确定性但将推理行为从读者面前隐藏起来。取而代之，让推理作为推理可见：陈述观察到的模式，清晰地表达解释，展示两者如何连接。语言应当遵循推论的性质而非一套固定模板——重要的是读者能够看到观察了什么、正在推断什么、以及两者之间的路径，而不让整个推理被折叠进一个躲避性的词中。
- 不要附加通用的 `如果后续……`、`未来可以……` 或类似的扩展段落，除非教师模板或用户明确要求未来工作内容。
